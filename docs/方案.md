# AIReelStudio 技术方案

> 最后更新：2026-09-17  
> **产品**：AI 短剧/短视频创作工作台 —— 三栏桌面应用（项目树 + 文档/素材/生成 + 嵌入式 Shell），对接本地/UNC 项目根与 Agent CLI。  
> **实现状态**：可用预览 **v0.1.1**（Windows 优先）。下文 **「十一、实现状态」以代码为准**；二～九节含目标架构，未实现项勿当作已交付。  
> **对外功能说明以 [README.md](../README.md) 为准。**

### 当前已交付（摘要）

- 三栏布局；中间栏分段切换（素材 / 文档 / 生成）且 IndexedStack 保活；主题/字号；窗口最大化记忆  
- 项目树：剧本/季/集解析、懒加载、过滤、Ctrl 多选、原地新建/重命名、进度徽章、周期扫描；标签「定位」会滚动居中；右键「在素材栏里查看」  
- 树与素材共用右键/快捷键/拖放（含从资源管理器拖入；Ctrl 复制）  
- Markdown：多 Tab、高亮、行号、大纲、预览切换、标签右键、工作区记忆（含选区）  
- 素材：网格 **与** 列表（列宽/排序持久化）、类型过滤、悬停预览、导入、截帧  
- Comfy：项目模板库 + 多 URL 绑定 + 生成面板；结果可跳回素材栏并高亮  
- 全局搜索（文件名 + MD，非 FTS）  
- Shell：多 Tab、`kyroon_pty`+flterm、快捷启动、cwd 策略、Agent 引用；OpenCode 会话列表（缓存刷新、行内改名/删除、TUI 内切换或 `-s`、仅存活智能体自动恢复、退出后软恢复宿主）  
- Windows Release zip + Inno 安装器（`scripts/pack_windows.ps1`）  

---

## 一、项目定位

创作流程里，剧本与物料多以**目录 + Markdown + 媒体文件**存放。本项目的目标是把这套结构**软件化**，而不是另起一套云端 CMS：

- **可视化呈现**：剧本树、物料网格、季/集层级浏览  
- **剧本管理**：多剧本组织与 Markdown 编辑  
- **物料管理**：角色定妆、场景、音频等文件夹的浏览与预览  
- **长剧支持**：多季、多集层级  
- **AI 互操作**：右侧嵌入式 Shell，快捷启动 opencode、dsh-tui 等；支持把当前文件/选区引用注入智能体  

用户配置的「项目根」通常是这类创作目录；与本仓库里用于打包的 `scripts/` 目录不是同一概念。

### 命名

- 产品名：**AIReelStudio**  
- 出发点：Reel（短视频/短剧）比 Video 更精准  
- Dart package 名：`ai_reel_studio`

---

## 二、技术选型

### 2.1 当前实际依赖（v0.1）

| 维度 | 选型 |
|---|---|
| 框架 | Flutter 3.x / Dart ^3.6 |
| UI | Material 3 |
| 状态 | Riverpod |
| 终端 | `kyroon_pty` + `flterm` |
| 媒体 | `media_kit` + video libs（Windows） |
| 配置/记忆 | SharedPreferences（含工作区、Shell 会话） |
| 窗口 | `window_manager` + `screen_retriever` |
| 选路径 | `file_picker` / `file_selector` |
| Markdown 预览 | `flutter_markdown`；编辑高亮自研 |
| 目录变更 | 自研周期扫描（非依赖 `watcher` 包事件） |

### 2.2 目标选型（尚未落地或仅声明）

| 维度 | 原目标 | 现状 |
|---|---|---|
| 路由 | go_router | pubspec 有依赖，业务未用 |
| 数据库 | drift / SQLite FTS | ❌ 未接入 |
| 文件监听 | `watcher` | 实际为轮询扫描 |
| PTY | `flutter_pty` | 已换 **`kyroon_pty` + flterm** |
| 图片预览 | photo_view | 现用 `InteractiveViewer` |
| 分层 | data/domain | 现为 `lib/core` + `lib/features` |

**为什么是 Flutter：** 后续 Android 路线、Hot reload、三栏工具型 UI；桌面优先 Windows。

### 2.3 平台优先级

```
Windows (主，正式打绿色包) → Linux (有工程壳) → Android (后续)
```

仓库当前无 `macos/` 目录。

---

## 三、整体架构

### 3.1 当前实现（v0.1）

```
┌────────────────────────────────────────────────────────────┐
│                    AIReelStudio (Flutter)                  │
│  ┌── UI ─────────────────────────────────────────────────┐ │
│  │  左：项目树     中：素材 / 文档 / 生成      右：Shell     │ │
│  │  过滤/新建/右键  网格·列表 / MD / Comfy      PTY 多 Tab  │ │
│  │  进度·拖放       媒体预览 / Ctrl+P          快捷启动·会话 │ │
│  └──────────────────────────────┬────────────────────────┘ │
│  ┌── 状态 ──────────────────────┴────────────────────────┐ │
│  │  Riverpod + SharedPreferences（工作区 / Shell / 进度）   │ │
│  └──────────────────────────────┬────────────────────────┘ │
│  ┌── 核心服务 ──────────────────┴────────────────────────┐ │
│  │  DirectoryParser / DirectoryWatcher(轮询) / Search     │ │
│  │  WorkspaceMemory / ShellSessionMemory / AgentBridge    │ │
│  └──────────────────────────────┬────────────────────────┘ │
│  ┌── 数据 ──────────────────────┴────────────────────────┐ │
│  │  dart:io 文件系统 = source of truth（无 SQLite）        │ │
│  └───────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

**原则（已落地）：文件系统是唯一事实来源；偏好与会话记在 SharedPreferences。**  
外部改文件 → 周期扫描刷新 UI（非可靠 OS 事件）。

### 3.2 目标架构（未落地）

远期曾规划 Repository / drift 缓存 / `watcher` 事件 / 角色与生成清单专页等；详见 §四.2、§十一 ❌ 项。勿与上图混淆。

---

## 四、数据模型

用户**项目根**下的创作目录约定（与本仓库 `scripts/pack_*.ps1` 无关）：

```
Project (可配置根目录)
├── Script (剧本)                → 001_悬疑_最后一次通话/
│   ├── meta.json（约定，进度现多用本地偏好）
│   ├── 剧本.md / 分镜.md …
│   ├── Material                 → 角色定妆照/、场景素材/ …
│   └── Season / Episode [可选]  → 剧集/1_觉醒/01_…
└── Shared（共享资源，按目录组织即可）
```

### 4.1 目录识别规则（已实现于 `DirectoryParser`）

```
Script / Season / Episode: 序号前缀目录约定 + 含剧本.md 等启发式
Material: 目录内媒体等非 .md 文件归到素材视图
```

### 4.2 SQLite 元数据 (drift) — 目标，未实现

```
tables: scripts / seasons / episodes / materials / start_cmds / sqlite_fts
```

当前：进度、快捷启动、工作区、Shell 会话均在 SharedPreferences；搜索为内存扫描。

---

## 五、Shell 集成设计

```
┌─ 右面板 Shell ─────────────────────────────────────────┐
│  [opencode] [dsh-tui] [bash] [+]       ← Tab 栏        │
├────────────────────────────────────────────────────────┤
│  $ opencode                                           │
│  > 进入 opencode 交互界面...                            │
│  > 用户直接在 app 内与 AI Agent 对话                     │
│                                                        │
├────────────────────────────────────────────────────────┤
│  快捷栏: [opencode] [dsh-tui] [ComfyUI] [自定义 +]     │
└────────────────────────────────────────────────────────┘
```

### 实现要点

1. **PTY**：`kyroon_pty`（Windows ConPTY）+ `xterm` 渲染  
2. **多 Tab**：每 Tab 独立 `TerminalSession`  
3. **快捷启动**：SharedPreferences 存 `StartCmd`（name/command/cwd 策略），非 SQLite  
4. **cwd 联动**：可按「项目根」或「当前选中目录」启动  
5. **Agent 引用**：可配置快捷键，向活跃智能体填入 `@相对路径#L…`（不回车）  
6. **会话恢复（一期，已实现）**：按项目持久化 Tab 列表、cwd、启动命令、面板显隐；重启后重建 PTY 并重拉命令；**不恢复**滚动缓冲与进程本身  
7. **Shell 类型**：Windows 默认 PowerShell  

---

## 六、核心功能模块

### 6.1 项目树导航（左面板）

**已实现：**
- 树形展示项目根下剧本，识别扁平 / 多季多集  
- 过滤；新建顶层剧本、新建子文件夹；右键打开、资源管理器、重命名、复制、删除、设置创作进度  
- 进度徽章（本地偏好，非 meta.json）  

**目标尚未实现：**
- 树内 SQLite FTS；「在终端打开」等未接线项以 UI 为准  

### 6.2 工作区（中面板）

**决策：不做左右分屏预览**（省屏幕）；当前实现为 **编辑 ↔ 预览切换**，另有大纲轨与行号。

**已实现：**
- Markdown 多 Tab 编辑（自研高亮）、大纲、行号、工作区记忆  
- 物料网格浏览、导入、类型过滤、媒体预览与截帧  

**目标尚未实现：**
- 网格/列表切换、拖拽上传/移动、标签  
- 角色管理专页、生成/音效清单专页、音频波形  

### 6.3 嵌入式 Shell（右面板）

- 多 Tab、快捷启动、可折叠（折叠保活）  
- 会话一期恢复见上文「五」  
- Agent 文件/选区引用快捷键  

### 6.4 远程目录支持

- 设置中配置项目根（UNC / 绝对路径）  
- OS 层挂载；应用不实现 SMB/SSHFS  
- 目录刷新：周期扫描（非可靠 file watcher 事件）  

---

## 七、当前代码结构（v0.1）

```
ai_reel_studio/
├── lib/
│   ├── main.dart / app.dart
│   ├── core/                 # config、解析、记忆、搜索、进度、Agent 桥等
│   └── features/
│       ├── layout/           # 三栏主界面
│       ├── tree/             # 项目树
│       ├── editor/           # Markdown
│       ├── asset/            # 素材网格
│       ├── media/            # 媒体预览
│       ├── shell/            # 终端
│       ├── search/           # Ctrl+P
│       └── settings/
├── scripts/pack_windows.ps1  # 打绿色包
├── installer/windows/        # Inno Setup
├── windows/ linux/ android/
├── docs/
└── README.md
```

> 下列原「目标分层」（`data/`、`domain/`、`characters/` 等）尚未落地，仅作远期参考。

---

## 八、技术调研风险点（Phase 0，历史）

| 风险点 | 结果 / 应对 |
|---|---|
| PTY Windows | 已采用 `kyroon_pty`，可跑 opencode 等 TUI |
| `xterm` Unicode/性能 | 可用预览可接受 |
| 网络目录事件 | 采用周期扫描降级 |
| 桌面视频 | `media_kit` |
| Markdown 编辑 | TextField + 自研高亮 + 可选预览切换 |

---

## 九、Roadmap（规划对照，非说明书）

```
Phase 0–1: PoC + 骨架          ✅
Phase 2: 物料/搜索增强         🟨（拖拽/列表已有；仍缺标签、FTS）
Phase 3: Shell                 ✅ 主体 + OpenCode 会话面板（恢复仍无滚动缓冲）
Phase 4: 长剧/进度/meta.json   🟨（季集与徽章有；非 meta.json）
Phase 5: 打包与稳定性          ✅ zip + Inno（需本机 Inno Setup）
Phase 6: Android               ⏳
```

对外功能说明以 [README.md](../README.md) 为准。

---

## 十、待确认事项

| # | 事项 | 状态 |
|---|---|---|
| 1 | 技术栈选型 | ✅ Flutter |
| 2 | Markdown | ✅ 非分屏；现为编辑/预览切换 |
| 3 | ComfyUI API 内嵌 | ✅ 模板库 + 多 URL 绑定，见 [ComfyUI集成方案.md](./ComfyUI集成方案.md) |
| 4 | 远程目录 | ✅ OS 挂载 + UNC/绝对路径 |
| 5 | 项目名 | ✅ AIReelStudio |
| 6 | Android | ✅ 后续阶段 |

---

## 十一、实现状态（相对本方案，以代码为准）

| 能力 | 状态 | 备注 |
|---|---|---|
| 三栏布局 / 主题 / 窗口最大化记忆 | ✅ | 中间栏分段主切换 + IndexedStack 保活 |
| 项目树（剧本/季/集解析） | ✅ | `DirectoryParser`；懒加载；Ctrl 多选 |
| 树定位滚动 / 跳转素材栏 | ✅ | `treeRevealRequest`；`viewInAssets` |
| Markdown 编辑 + 高亮 + 行号 + 大纲 | ✅ | 预览为切换，非分屏；标签右键 |
| 工作区记忆（Tabs/树/光标/滚动/选区） | ✅ | `WorkspaceMemory` |
| 设置：项目根 / 字号 / 快捷启动 / 引用键 / Comfy URL | ✅ | SharedPreferences |
| 物料网格 + 列表 + 右键 + 导入 | ✅ | 列宽/排序持久化；无标签系统 |
| 应用内拖放 + 外部拖入 | ✅ | `desktop_drop`；Ctrl 复制 |
| 图片/视频/音频预览、悬停预览、截帧 | ✅ | media_kit + opencv_dart |
| 全局搜索（文件名+MD） | ✅ | 非 SQLite FTS |
| PTY 多 Tab Shell | ✅ | kyroon_pty + flterm |
| 快捷启动 + cwd 策略 | ✅ | 智能体 Tab 避免多余 Set-Location |
| Agent 文件/选区引用 | ✅ | 可配置组合键 |
| OpenCode 会话面板 | ✅ | 列表缓存、改名/删除、TUI 切换或 `-s` |
| Shell 会话恢复 | ✅ | 仅退出时仍存活的智能体自动再启；无滚动缓冲；OpenCode 退出后软恢复宿主 |
| 创作进度徽章 | 🟨 | 本地偏好，非 meta.json |
| 文件标签 / 角色库 / 生成清单 | ❌ | |
| ComfyUI 动态生成 | ✅ | 模板库 + 多 URL 绑定 + 回跳素材；见 [ComfyUI集成方案.md](./ComfyUI集成方案.md) |
| drift / SQLite FTS | ❌ | |
| Windows Release zip / Inno | ✅ | `scripts/pack_windows.ps1`；Inno 需本机已装 |
| Linux/macOS 正式分发 | ❌ | 无 macos/ 工程 |
| Android | ❌ | 后续 |

---

*本文随实现推进更新；对外说明以仓库根目录 [README.md](../README.md) 为准。*
